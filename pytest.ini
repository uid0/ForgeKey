[pytest]
testpaths = tests
markers =
    hil: hardware-in-the-loop tests that require a real ForgeKey smoke evidence bundle
    simulator: Wokwi-compatible simulator contract tests
    host: host-side protocol and contract unit tests
