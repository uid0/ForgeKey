// Template for predefined dev WiFi networks.
//
// Copy this file to `secrets_local.h` (same directory) and fill in your
// own SSIDs/passwords. `secrets_local.h` is gitignored — never commit
// real credentials.
//
// On boot, captive.cpp tries these networks via WiFiMulti before falling
// back to the captive-portal AP. Define one or both; undefined entries
// are skipped at compile time.

//#define FORGEKEY_NETWORK_1_SSID     "YourDevSSID"
//#define FORGEKEY_NETWORK_1_PASSWORD "YourDevPassword"

//#define FORGEKEY_NETWORK_2_SSID     "YourBackupSSID"
//#define FORGEKEY_NETWORK_2_PASSWORD "YourBackupPassword"
